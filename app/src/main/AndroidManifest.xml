<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.covidtracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CovidTracker"
        android:usesCleartextTraffic="true"
        tools:targetApi="21">
        <activity android:name=".SplashActivity"></activity>
        <activity android:name=".CountryDetailActivity" />
        <activity android:name=".DistrictDetailsActivity" />
        <activity android:name=".DistrictWiseActivity" />
        <activity android:name=".StateDetailsActivity" />
        <activity android:name=".StateWiseDataActivity" />
        <activity android:name=".CountryDataActivity" />
        <activity android:name=".WorldDataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>